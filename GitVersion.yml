workflow: GitHubFlow/v1
mode: ContinuousDeployment
branches:
  main:
    regex: ^main$
    increment: Patch
  develop:
    regex: ^develop$
    increment: Minor
    label: beta
  feature:
    regex: ^feature[/-]
    increment: Inherit
    label: alpha.{BranchName}
  release:
    regex: ^release[/-]
    increment: None
    label: rc
